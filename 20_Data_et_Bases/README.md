# 20 — Data et Bases

Ce dossier accueille les schémas de données, dictionnaires anatomiques, checkpointers d'état et modèles de persistance.

- `state_schema.json` : Schéma canonique de l'état runtime de l'application Skelet (SKILL.state).
- Schéma LocalStorage : Clé `skelet.observations.v1`.
- Catalogues de données : `dist/models/*-catalog.json`.
